# 2026-09-03 工作日志

- 用户替换了 7 张主要角色立绘：`char_lizhi.png`、`char_wenyin.png`、`char_xuqinglan.png`、`char_yanchengfeng.png`、`char_zhangchengyu.png`、`char_zhaoheng.png`、`char_zhouwen.png`；已安全同步远端五页场景 `.meta` 提交后，用当前立绘提交并运行构建 `72cb747`（`chore: verify updated character portraits`），Maker 构建成功、watcher `lastError=null`、本地运行日志文件尚未生成新会话；用户的工作记忆和旧候选图未混入提交。
- 读取 Wolai `修改反馈` 的 1-5：已修正音乐持续循环到 `opening_prologue_5_after` 结束后才淡出；所有对话首句含“黑屏显示：...”的转场统一改用黑底白字 `ShowDateCard` 并跳过说明句；`dialogues.csv` 新增 `portrait_position`（1左/2右），CSV Loader、编译器和 `DialogueSystem` 已接入，且修复 `startLine` 参数真正从指定行开始；开场背景镜头取消推拉缩放与横移，交互物悬停放大 20%/边缘光、按下恢复原尺寸、松开恢复放大、移出复原。已重新生成 `scripts/data_dialogues.lua`，静态检查通过（仅项目既有引擎全局/UI 类型警告），尚未提交或构建。
- 用户要求“拆吧”：已为大堂五页生成独立透明交互贴图（雨伞架、展架、签到簿、外卖柜、假山、电梯、垃圾桶、安全通道），并生成移除原物件的五张干净背景；代码改为每个大堂交互物使用 `interactiveSprite` 独立渲染，悬停/按下时贴图本体同步缩放和显示边缘光，线索热区继续沿用原分页位置。已完成资源复制、代码接入和静态校验，尚未提交或构建。
- 用户补充“第一章的线索内容也要拆”：已为第一章庭院、25楼走廊和 2501 案发现场的所有线索物件生成独立透明贴图，并生成对应清理背景；接入 `GameData`，包括盆栽、茶歇长桌、路由器、电源桩、喷泉中控箱、4 个房门、温控面板、床头柜、尸体位置。原有分页热区未改，均使用 `interactiveSprite=true`；静态校验和资源引用检查通过，尚未提交或构建。
- 用户补充“序章的也需要”：已为事务所客厅与卧室全部可交互物件生成独立透明贴图（办公桌、台灯、两张沙发、餐桌、灶台、卧室桌椅、床、双衣柜），并生成 `prologue_livingroom_clean.png`、`prologue_bedroom_clean.png` 两张清理背景；`GameData.office` 已接入全部 `interactiveSprite=true`，保留原分页和热区位置。静态检查尚未重新构建或提交。
- 用户要求跑一下；已提交并推送 `bf6f37b`（`feat: split interactive objects across all scenes`），包含序章、第一章、大堂的独立交互贴图和清理背景，以及交互代码/黑屏转场/音乐/对话站位改动。Maker 远程构建成功、预览刷新成功，运行日志进入主菜单并开始序章，错误搜索无 Lua/资源错误，watcher `lastError=null`；浏览器预览已打开。临时生成候选图和工作记忆未混入提交。
- 用户反馈序章截图中物件位置偏移且选中边缘描边缺失；已为稳定序章物件生成 `_trim.png` 裁切贴图和 `_outline.png` 金色轮廓贴图，`SceneManager` 支持按 `spriteTrim` 使用裁切贴图，并将独立物件选中描边改为独立轮廓层；序章物件接入 `spriteTrim=true`，同步调整物件缩放参数。静态检查仅新增引擎 `fileSystem` 全局警告，尚未构建或提交。
- 用户要求运行修正版；已安全同步远端自动生成 `.meta` 提交 `4951298` 后提交并推送 `5724907`（`fix: align prologue objects and outlines`）。Maker 构建成功，运行日志进入主菜单，`lastError=null`，错误搜索无 Lua/资源加载错误，预览已打开。
- 用户继续反馈事务所物件大小/位置仍不对；已让 `SceneManager` 支持每个交互物独立 `spriteRect`，并为序章客厅/卧室按截图重新设置桌子、台灯、沙发、餐桌、灶台、卧室桌椅、床和衣柜的渲染矩形；序章屏幕背景改用 `backgroundFit="fill"`，使背景与设计坐标一致；保留原 hitbox 不变。序章描边层改用裁切后的 `_outline.png`，静态检查通过，尚未构建或提交。
