# 2026-09-01 工作日志

- 用户新增 9 张次要角色立绘并要求替换：已将其映射为稳定资源 `char_guard/doctor/waiter/waiter_b/police/sister/pangan_employee_a/pangan_employee_b/receptionist.png`；`DialogueSystem.portraitMap` 已补 `WaiterB` 和磐安员工甲乙，第一阶段开场的两名磐安员工改用甲乙独立立绘，现有询问/场景 NPC 引用自动使用被替换后的稳定文件。
- Maker 构建成功提交 `ed05199`；远端自动生成并已拉取新增资源 `.meta` 同步提交 `3f021fd`。运行日志进入主菜单且无 Lua/纹理错误。
- 用户要求对话立绘只显示半身；`DialogueSystem.lua` 将对话立绘框改为 `260x360`、`backgroundFit="cover"`、`backgroundPosition="center top"`、顶部裁切，场景中的全身 NPC 立绘不变。构建成功提交 `a1a3fb8`，最新日志无 Lua/纹理错误。
- 用户反馈半身裁切效果不对，改为完整比例放大并贴屏幕底部：对话立绘框 `320x540`、`backgroundFit="contain"`、`left=24`、`bottom=0`，由底部对话面板自然遮住下半身。构建成功提交 `50bf25d`，最新日志无 Lua/纹理错误。
- 用户要求对话立绘再放大 1.5 倍、下移，并让对话 UI 覆盖立绘：`DialogueSystem.lua` 调整为 `480x810`、`bottom=-135`、portrait `zIndex=10`，对话面板 `zIndex=100`。构建成功提交 `71d4eb1`，最新运行日志无 Lua/纹理错误。
- 用户要求第一阶段从时间地点黑屏开始播放 `朝のラーメン.mp3`，序章对话结束进入线索探索时减弱并停止；复制为稳定资源 `assets/audio/first_stage_theme.mp3`，新增 `scripts/MusicSystem.lua`（音乐循环、0.32 音量、1.2s 渐入、1.8s 淡出），接入 `main.lua` 的 `StartNewGame`、`EnterPrologueScene`、`EnterMainMenu` 和更新循环。Maker 构建成功提交 `8e11065`，MP3 与 MusicSystem 的 `.meta` 已同步提交 `84d999c`；远端构建成功且无运行错误。
- 用户要求去掉所有场景中的人物立绘，只保留对话框立绘；`SceneManager.showSceneCharacterSprites=false` 禁止探索场景 NPC 立绘，`OpeningSystem.showSceneActors=false` 禁止开场分镜角色层，交互和对话立绘逻辑保留。构建成功提交 `f4416fc`，运行日志进入序章且无 Lua/纹理错误。
- 用户要求回忆切入显示纯黑日期卡；`OpeningSystem` 新增 `ShowDateCard`，回忆段在 `opening_prologue_3` 前显示居中的 `2036年8月8日 14:20` 黑屏 3 秒，随后从该段第 2 行开始对话，避免原说明文字进入对话框。`DialogueSystem.Start` 新增可选 `startLine` 参数。构建成功提交 `07d59eb`，无 Lua/纹理错误。
