# 2026-08-27 工作日志

## 序章/第一章场景黑屏（真因定位 + 修复）
- **现象**：进入序章 office / 第一章 lobby 等 screens 场景整屏纯黑，无报错。此前（08-26）因 `_makeItemBtn` 崩溃从未真正渲染过场景，修好崩溃后才暴露黑屏。
- **误判与纠正**：先误判为"SceneManager root 未挂载渲染树"，加 `UI.GetRoot():AddChild(root)` 无效。真正根因：**引擎 `Widget:new(props)` 仅接受单参数 `props`（`urhox-libs/UI/Core/Widget.lua:308`），且不处理 `props.parent`**。SceneManager 大量 `UI.Panel(parent, {props})` / `UI.Label` / `UI.Button` 双参数调用，第二参数 `props` 被完全忽略 → 所有控件以**空 props** 创建（无尺寸/背景/样式）→ 不渲染 → 黑屏。Menu/Opening 用单参数 `UI.X({...})` 故正常能显示。
- **修复**：SceneManager 顶部加兼容 wrapper `Panel/Label/Button`，把 `UI.X(parent, props)` 转为 `ctor(props)` + `parent:AddChild`（兼容 `X(props)`/`X(parent,props)`/`X(nil,props)` 三种形式），并 `replace_all` 切换调用。EnterScene 的 root 仍显式 `UI.GetRoot():AddChild(root)`。commit 7318552，构建 100%，lint 0。
- **结论**：08-26 日记第 6 次的"root 未挂载"判断是错误的，真正坑是"双参数 UI 构造被引擎吞掉第二参"。已修正 MEMORY.md。排查 UI 黑屏第一优先级：确认构造调用是否误用双参数 `(parent, props)`。

## 序章开场动画加跳过按钮
- 需求：所有纯对话剧情加跳过按钮（如序章）。
- 实现：`OpeningSystem` 加 `_AddSkipButton(parent)` helper，在 `BuildLocationUI`（location 阶段）和 `BuildBackdrop`（dialogue 阶段）都挂 "跳过 ›" 按钮（右上角，点击 `M.Finish()` 直接结束开场进入场景）。`Update` 开头有 `if not M.state.active then return end` 防护，跳过安全。commit 7318552。
- **DialogueSystem 跳过按钮（2026-08-27 已加）**：用户确认对话界面也需跳过。在 `DialogueSystem.BuildUI` 加左上角"跳过 ›"按钮（top:16,left:16，避开 OpeningSystem 右上角"结束整个开场"按钮），点击 `M.End()`（结束整段对话 → 调 onComplete 进下一节点）。`M.End` 内置 `onComplete` 置空防护，重复点击安全。commit 80cdd7e，构建 100%。序章对话阶段两按钮并存：右上(OpeningSystem)=结束整个开场，左上(DialogueSystem)=结束当前分镜。

- **OpeningSystem 局部函数 nil 报错修复（2026-08-27）**：用户反馈 `scripts/OpeningSystem:111 attempt to call a nil value (global '_AddSkipButton')`。根因：`_AddSkipButton` 是 `local function`（原定义在 BuildLocationUI/BuildBackdrop 两个调用点**之后**），Lua 局部函数前向调用时变量尚未初始化= nil。修复：将定义上移到 `BuildLocationUI` 之前（所有调用点之前）。commit 662ffe2，构建 100%，lint 0。

- **双跳过按钮去重（2026-08-27）**：用户截图显示序章对话阶段同时出现左上(DialogueSystem) + 右上(OpeningSystem)两个"跳过 ›"按钮。修复：`DialogueSystem.Start` 加第三参数 `showSkip`（默认 true），`BuildUI(showSkip)` 条件创建跳过按钮；`OpeningSystem.PlayNextDialogue` 调用时传 `false` 隐藏 DialogueSystem 跳过，只保留 OpeningSystem 右上角"结束整个开场"按钮。commit 7487b1a，构建 100%。

- **场景 UI 两问题修复（2026-08-27）**：用户截图反馈：(1) 左上角标题"黄志事务所 · 九八点夜"纯白字浮在图上无背景 → 给 titleLabel 加半透明深色背景(rgba(0,0,0,140)) + 圆角6 + 内边距；(2) 右上角角色立绘只露出脚 → 根因 charY 公式错误 `(1-cp.y)*sh-charH` 当 y=0.78 时算出负值(-274) → 改为 `sh*cp.y-charH`。commit f783a72。

- **场景 UI 三问题修复 v2（2026-08-27）**：用户再次截图反馈三问题：(a) 角色站在中间很突兀 → screens 模式下 charSprite 设 `visible=false`（2D横板推理探索模式不需要角色立绘遮挡场景和交互UI）；(b) 解密交互UI（翻页按钮/小地图/物件）消失 → 全局修复 `color`→`fontColor`（翻页按钮◀▶/小地图标签/hoverNameLabel/教程文字/滚动提示共6处，`color` 非标准 Label 属性导致文字不可见=按钮看起来像消失了）；(c) 对话时跳过点不了 → titleLabel 从左上移到右上角(`right:12`+`textAlign:right`)避开 DialogueSystem 跳过按钮(左上16,16)。commit 05e5627，构建 100%。⚠️ 教训：Label 文字颜色属性名是 **fontColor** 不是 color；多系统叠加 UI 时必须做位置冲突检查。
