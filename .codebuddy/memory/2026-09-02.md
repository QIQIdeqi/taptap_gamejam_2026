# 2026-09-02 工作日志

- 按“未被 Lua/CSV/配置引用 + 明显旧版/候选/测试素材”标准清理资源：删除旧版 `bg_c1~c5`、`bg_e1~e6`、`bg_p1~p5`、等距序章图、旧事务所图层、旧 UI/线索候选、CG 首帧候选、测试截图、未使用 cgt 视频分段及其 `.meta`，以及已复制为稳定资源的原始日文 MP3和中文旧立绘源文件。
- 保留正式场景、线索图、角色立绘、`opening_cg.mp4`、`assets/audio/first_stage_theme.mp3` 及其运行所需资源。
- 执行 `git pull --ff-only origin main`，结果为 Already up to date；远端当前提交 `60e4242`。清理删除尚未单独提交，用户未提交的 `char_zhangchengyu.png` 和工作记忆文件保留。
- 用户确认“可以跑一下，可以提交”；已暂存并恢复无关用户改动后提交素材清理，Maker 构建成功并推送 `5e1a2e1`，本地与 `origin/main` 一致。清理提交仅包含已确认未使用素材删除；工作记忆和 `char_zhangchengyu.png` 恢复为未提交状态。
- 读取 Wolai `2.1 场景1：万丽海湾大酒店内 大堂`：按平面图将旧的 4 张整图翻页改为单一连续横版卷轴，生成入口/中央假山展架/前台庭院入口三段背景并拼接为 `assets/image/lobby_scroll_panorama.png`；`hotel_lobby` 改用 `parallax` 世界坐标，假山、展架、外卖柜、VIP签到簿和两个出口热区已重排。`SceneManager` 新增世界热区随镜头滚动、底部卷轴进度条与操作提示，并将场景标题移到左上、悬停名称移到下方；已完成静态校验，尚未提交或远程构建。
- 用户要求运行并在浏览器打开；已按指定文件提交并推送 `f9a97eb`（`feat: convert hotel lobby to horizontal scroll`），Maker 远程构建成功、预览刷新成功，浏览器已打开 `https://maker.taptap.cn/app/6c5d6252-8c88-49d3-980c-d47d8c8495f0?localDev=1`。watcher 状态 `lastError=null`，但本地 `runtime.log` 尚未产生新会话文件；未提交的工作记忆、`char_zhangchengyu.png` 和三张候选原图仍保留。
- 用户指出大堂右段缺少电梯、垃圾桶和安全通道；已依据平面图重新生成 `lobby_scroll_right_facilities_20260902130619.png`，补齐三组电梯、两只垃圾桶、右下安全通道及绿色出口灯，并重建 `assets/image/lobby_scroll_panorama.png`（5820x1083）。`GameData.hotel_lobby` 新增三部电梯、两只垃圾桶、安全通道的交互热区，以及电梯/安全通道通往 25F 的出口；静态检查通过，尚未重新构建或提交。
- 用户说明横版卷轴不必限制三图，可拆成四到五图；据此重新生成五段 `lobby_scroll_01_entrance` 至 `lobby_scroll_05_safety`，按入口→前台→假山→电梯厅→安全通道拼接为 `assets/image/lobby_scroll_panorama.png`（9700x823），并将 `GameData.hotel_lobby.worldWidth` 与所有物件/出口热区同步到五段世界坐标；静态检查通过，尚未构建或提交。
- 用户要求改进代码并牢记线索交互位置；`SceneManager` 已将酒店大堂物件/出口统一挂到 `_worldLayer`，镜头只移动世界层，避免逐个热区随镜头修正造成漂移；五个正式线索热区已对应入口/前台/假山段坐标，电梯与安全通道出口热区缩小到门底，避免覆盖调查热区。静态检查仅保留原有引擎全局变量警告，尚未构建或提交。
- 用户明确“拼接长图直接用翻页的形式”；已将 `GameData.hotel_lobby` 从 `parallax` 长图改为 `screens` 五页翻页，直接引用 `lobby_scroll_01_entrance` 至 `lobby_scroll_05_safety`。每页独立承载对应线索热区：正门/展架、前台签到簿/外卖柜、中央假山、三部电梯/两只垃圾桶、安全通道；出口分别放在庭院入口、电梯底部和安全门底部。旧 `lobby_scroll_panorama.png` 已不再被代码引用，尚未构建或提交。
- 用户要求运行；已提交并推送 `cbba6c4`（`feat: use paged lobby backgrounds`），Maker 构建和预览刷新成功，浏览器已打开 `https://maker.taptap.cn/app/6c5d6252-8c88-49d3-980c-d47d8c8495f0?localDev=1`。运行日志已进入序章，未发现 `ERROR`、资源加载失败或 Lua nil 错误，watcher `lastError=null`；工作记忆、`char_zhangchengyu.png` 和旧候选图未混入提交。
- 用户要求运行；Maker 远端先发现 `origin/main` 有一个仅包含 `lobby_scroll_panorama.png.meta` 的同步提交 `65010c4`，已安全 stash 未提交改动、快进同步并恢复；随后成功提交并构建 `fe1d7bd`（`feat: refine lobby scroll interaction layer`）。运行日志进入主菜单，`lastError=null`、watcher 无失败，预览已打开 `https://maker.taptap.cn/app/6c5d6252-8c88-49d3-980c-d47d8c8495f0?localDev=1`；用户工作记忆、`char_zhangchengyu.png` 和候选原图未混入提交。
