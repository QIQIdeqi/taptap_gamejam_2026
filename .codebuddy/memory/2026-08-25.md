# 2026-08-25 工作日志

## AI图像Prompt套装输出 ✅（2026-08-25）
- 用户要求根据wolai文案生成可直接用于AI图像生成的场景prompt套装。
- 提供了参考图（日系二次元动漫风格立绘——干净线稿、赛璐璐上色、修长比例、暖调低饱和）。
- 从wolai读取了基础设计文档（角色介绍）、第一阶段（序章场景）、第二阶段（4个探索区域详细描述）的完整内容。
- 输出 `docs/ai-image-prompts.md`：
  - 5张场景背景 prompt（S1黄昏事务所 / S2酒店大堂 / S3露天庭院 / S4 25F走廊 / S5案发现场）
  - 7张主角立绘 prompt（C1李志/C2陈雯音/C3许晴岚/C4严成峰/C5赵恒/C6周文/C7张承宇）
  - 每段均含主体描述/环境氛围/光线/风格/镜头角度，100词以内中文
  - 附带 TapTap Maker 批量调用参数示例 + 风格一致性检查清单
- **注意**：此风格与08-24已生成的12张图（现代写实插画）不同。若用户要求重新生成需用新prompt。
- 背景图已更新：用户当日用日系二次元风格重新生成5张背景图并覆盖 `assets/image/bg_*.png`，`git pull --autostash` 同步远端后构建成功（commit bc7dcac），watcher 健康无报错，预览正常。立绘 `char_*.png` 仍为写实风待重做。
- 背景图显示修复（重要）：用户反馈"背景还是空的"。排查发现 SceneManager.BuildUI 的 root panel 此前只设 `backgroundColor`（纯色），从未引用 `M.sceneBackgrounds` 加载 `backgroundImage`——图片从未被显示。已给 root panel 加 `backgroundImage = M.sceneBackgrounds[M.currentScene]` + `backgroundFit = "cover"`，lint 0，构建成功（commit 010e574），watcher 健康。场景背景图现在应正常显示（与 DialogueSystem 立绘同理，后者一直用 backgroundImage 故立绘正常）。
- 交互物件坐标对齐背景图：用户反馈"UI交互位置与图片不符合"。本会话无法重新视觉读取背景图像素（工具省略 base64），故依据 `docs/ai-image-prompts.md` 各场景的构图 prompt 推断物件在16:9画面中的归一化坐标，逐场景重排 `GameData.M.SceneObjects` 的 items/exits：office(书柜左/衣柜右上/床铺右下)、lobby(假山正中/展架左/外卖柜左/前台右)、courtyard(罗马柱左前/茶歇桌中下/电源桩最右/喷泉远中/WiFi梁上)、corridor(四门墙面水平排列中上部)、crime(尸体墙角地面/床头柜+温控同侧床头)。UI库不支持 aspectRatio，无法做自适应16:9舞台，故以16:9为基准+cover，在16:9预览下精确对齐；非16:9会按屏缩放（已知边界）。lint 0，构建成功（commit ebef964）。坐标系基于prompt推断而非逐像素视觉，建议用户在16:9预览确认，可能需微调。
