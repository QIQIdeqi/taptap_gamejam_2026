# 2026-08-28 工作日志

## 台词/线索 CSV 化（运行时读 CSV，可灵活编辑）
- 需求：把 GameData 里所有台词、线索文字提取成 CSV，方便策划在表格里改，Lua 直接读 CSV，且"按行数说多少句"。
- 产出：
  - `assets/data/dialogues.csv`（dialogue_id,line_no,speaker,portrait,clue,text,background，一行=一句）
  - `assets/data/clues.csv`（id,name,category,chapter,description,detail,image）
  - `scripts/CSVLoader.lua`（自写健壮 CSV 解析，支持 BOM/引号/逗号/换行；多路径回退）
  - `GameData.lua` 末尾 require CSVLoader 覆盖 M.Dialogues/M.Clues；找不到 CSV 回退内嵌兜底
- 验证：Node 侧冒烟测试还原 19 对话/110 行/29 线索正确；`maker_build_current_directory` 构建成功（commit a54c704）。
- 顺带修复 bug：序章办公室 4 对话(of_bookshelf/of_desk/of_bed/of_lamp_mislead) 此前被错误嵌套进 crime_ending_true 表，导致 M.Dialogues["of_bookshelf"] 为 nil、办公室物件点击无反应；CSV 化按 id 扁平化到顶层已修复。
- 删除 tools/gen_csv.js、test_loader.js，避免覆盖用户编辑的 CSV。

## 上一条请求（核对 wolai 台词）
- 已确认 wolai 基础设计页(3uc1XCmBM6jmyyAnVePCsU)是设计规格、不含对白台词；代码台词为 AI 据设计文档撰写，非 wolai 转录；git diff 近 9 次提交仅改 6 行坐标、台词 text 零改动。

## wolai 第二~四阶段缺失台词补入 CSV（2026-08-28）
- 用户确认对比结果后要求把 wolai 缺失台词全部补入 CSV。已拉取 wolai 二/三/四阶段含台词章节（get_section_content），按分镜逐条生成 dialogue_id 写入 `assets/data/dialogues.csv`。
- 新增 dialogue_id（共 12 组，+52 行；crime_found 由 5 句精简版扩为 10 句完整版）：
  - ch2_2501_eavesdrop（2501房偷听·严成峰+周文·3句）
  - ch3_npc_zhaoheng / ch3_npc_yanchengfeng / ch3_npc_xuqinglan / ch3_npc_zhouwen（庭院4组NPC·3/3/3/4句）
  - ch3_yanchengfeng_leave（严成峰回房·5句）
  - ch4_party_chat / ch4_discovery / ch4_elevator / ch4_police_check / ch4_zhang_visit（命案发现张承宇线·4/4/7/11/4句）
- 接入触发逻辑（避免死数据）：
  - GameData.lua：Characters 新增 WaiterA/WaiterB/PoliceA 三无立绘角色；courtyard 三屏加 NPC 站位物件（dialogueId 指向各 ch3_*）；corridor s2 加"门缝下的声响"物件(dialogueId=ch2_2501_eavesdrop)
  - main.lua：enter_crime 命案序列重构为完整链 party_chat→discovery→crime_found→elevator→police_check→zhang_visit→crime_scene
- 验证：CSVLoader 解析稳健（引号/逗号/空字段/无尾换行均兼容）；SceneManager._onItemInteract 原生支持 dialogueId(行478)；main/GameData lint 0 错误。
- 未做/待确认：① wolai 第三阶段 1.3 的 7 个场景物件(加急跑腿袋/网络中继器等)文字未以对话形式补入（它们是场景交互说明，已在 SceneManager 物件中以 interactText 形式存在）；② 运行时 Maker build 实测待用户触发；③ 个别明显笔误已按通顺化处理（如 ch4_elevator 第2句"么？"→"什么？"，严城峰/严成峰统一为 YanChengfeng）。
