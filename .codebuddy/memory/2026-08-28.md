# 2026-08-28 工作日志

### 冒烟测试污染 GameState 修复（01_28 凌晨）
- **根因**：`main.lua:105-109` 每次 Start() 自动运行冒烟测试，遍历5场景+收集全部线索，导致游戏不从序章(office)开始而直接进入大堂/庭院状态
- **修复**：加 `_ENABLE_SMOKE` 开关（默认关闭），需手动设 true 才跑测试
- **副作用发现**：翻页按钮点击触发两次 _SwitchScreen（14个 Nav FAIL），待修
