# 2026-09-05 工作日志

- 读取 `00_文档/Locate TapTap skill.pdf`：PDF 为单页扫描图，未包含可提取文本；已提取页面图像并按区域放大阅读。确认与当前项目相关的长期决策：游戏 UI/人物录入应通过 Tab 打开“侦探笔记”，不是开始界面或剧情里的角色立绘；笔记默认可从开场剧情结束后进入事务所自由探索，Tab 打开后点击左侧“人物录”分类，再点击人物，右侧查看人物档案；Tab 或 ESC 关闭，剧情、过场和询问中不能打开。当前代码已收录李志、陈雯音等人物；新增“人物录”后应检查 `NoteSystem` 分类标签、对象列表和右侧详情的显示与点击事件，避免只有提示面板而没有交互。
- 检查双 Git 远端同步状态：Maker `origin/main` 与当前 `HEAD` 同为 `a8424d8`；GitHub `github/main` 为不同提交 `7aced37`。两边提交历史分叉（共同基点 `0885b66`，拓扑计数为当前侧 208、GitHub 侧 5），但经过树对象和文件差异核对，`HEAD` 与 `github/main` 的最终文件树完全一致，当前差异仅是提交历史/哈希不同；工作区仍有本地记忆修改和未跟踪文件，尚未执行历史对齐或推送。
- 用户要求再次读取 Figma 节点 `2031-6` 并把黄昏事务所客厅/卧室背景、线索图片、位置和大小同步到序章；本次 FigmaPartner 的 `get_design_context`、`get_screenshot` 和 `get_full_bundle` 均返回 `fetch failed`。核对本地代码发现同一 Figma 布局此前已同步：`GameData.lua` 使用 `prologue_livingroom_clean.png` / `prologue_bedroom_clean.png`（1940×823），并保存 Figma 映射后的 10 个物件坐标与 `spriteRect`；本次未在无法读取最新 Figma 的情况下覆盖现有布局。
- 用户提供截图确认 Figma 中客厅和卧室确实已有新修改：客厅为大窗、左侧办公桌、中间沙发、右侧餐桌/厨房；卧室为左侧床铺、右侧衣柜。再次重试 Figma MCP（含 `2031-6`、`2031:6` 两种节点格式）仍为 `fetch failed`，因此问题是 Figma 连接器/服务请求失败，不是用户没有修改或链接无效；截图只能确认视觉变化，不能替代原始节点资源下载。
- Figma MCP 重新连接后恢复：读取 `2031:6` 客厅和 `2031:11` 卧室成功，并下载 2 张背景与 6 张独立物件 PNG。已替换序章正式背景/物件资源，并将 `GameData.lua` 的 `designW/designH` 改为 `1928×819`，按 Figma 背景内相对坐标更新餐桌、办公桌、沙发、卧室桌、床、衣柜的 `x/y/w/h` 与 `spriteRect`；非 Figma 独立层的旧台灯、灶台、右侧沙发、卧室椅子保留为无贴图交互热区，避免与新背景重复绘制。静态检查无新增错误，尚未构建提交。
- 用户要求运行 Figma 最新序章布局；Maker 工具两次等待远程结果超时，但本地提交 `36b1726` 已确认推送到 `origin/main`，预览地址已打开；本地运行日志未出现错误，但日志时间早于本次提交，远程新构建最终完成状态仍需 Maker 后台更新确认。
- 用户要求再次运行并强调屏幕适配；已尝试构建当前已提交版本（不提交本地记忆/未跟踪文件），Maker 客户端再次等待超时；本地和 `origin/main` 仍为 `36b1726`，watcher 状态文件保持 `lastError=null`，预览地址已打开，现有运行日志无错误匹配。
- 用户对比游戏截图与 Figma 截图，确认序章客厅/卧室存在视觉差异；已用 Figma 最新资源和 `GameData.lua` 坐标合成图核对：Figma 设计图包含背景与独立物件分层，而游戏截图还叠加了运行时 UI/交互贴图，且部分旧交互热区仍保留。当前 Figma 坐标映射与背景设计尺寸已一致，后续若要完全视觉一致，应进一步清理不在 Figma 独立层中的旧物件贴图，并校准运行时导航/HUD，而不是继续缩放背景。
- 用户明确当前序章对比范围：只关注背景图片、独立物件的摆放位置/大小，以及可点击热区；顶部标题、翻页按钮、底部提示等 HUD/导航 UI 不纳入本轮校准范围。
- 用户特别强调序章背景/物件/热区校准必须考虑屏幕适配；后续统一基于 Figma 背景设计尺寸和 `SceneManager._screenTransform` 映射，确保背景、独立物件与热区使用同一套 `cover/contain` 缩放及偏移，不能只按单一截图写死像素坐标。
- 用户反馈游戏内位置仍然是乱的，已重新用 Figma 原始节点校验并修正：客厅/卧室背景改为 `backgroundFit="fill"` 并保留 `designW=1928/designH=819`，使背景拉伸方式与物件映射完全一致；按 Figma 背景矩形原点重算并更新台灯、灶台、沙发边位、卧室椅子的热区，使其与新的桌/餐桌/沙发/床/衣柜坐标处在同一坐标系。静态检查无新增错误，尚未构建提交。
- 用户要求运行热区修正版；已提交并推送 `2ce01c0`（`fix: align prologue hotspots with background fit`），Maker 客户端等待远程结果超时，但远端 `main` 已确认指向该提交，预览地址已打开；本地运行日志未出现错误，但日志仍为旧版本运行时，远程新构建最终完成状态仍需 Maker 后台更新确认。
- 用户希望 GitHub 也推送成功，并授权强制对齐 GitHub `main`；已先备份 GitHub 旧 `main` 为远端分支 `backup/github-main-before-align-20260905`。强制推送被 GitHub 拒绝：历史中的 `assets.zip` 达 439.42 MB，超过 GitHub 100 MB 单文件限制。当前 `assets.zip` 仅在工作区未跟踪，但已存在于本地提交历史，因此不能直接把现有历史推送到 GitHub，需要先重写历史或改用 Git LFS。
